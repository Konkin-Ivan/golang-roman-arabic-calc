version: '3.x'

services:
  golang:
    build:
      context: .
      dockerfile: docker/go/app/Dockerfile
    volumes:
      - ./:/app
    entrypoint: [ "tail", "-f", "/dev/null" ]
    container_name: "${PROJECT_NAME}_go"